@using System.Data
@using MvcCheckBoxList.Model
@using Snoopy.Core
@model   Snoopy.Web.Models.ConstitutionViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="well">

    <h4>体制测试</h4><hr />



</div>
@*<div class="container-fluid margin-top">
        <div  >
            <div class="span6">
                <div>
                    <div class=" content-wrapper">
                        @foreach (var item in Model.Items)
                        {
                            <div class="alert alert-info" style="margin-left: 20px">
                                @Html.Label(item.Key)
                                @Html.CheckBox(item.Key, item.IsChecked)
                            </div>
                        }
                    </div>
                </div>
                <input type="button" value="保存" class="btn btn-primary" />

            </div>
        </div>
    </div>*@
<div>
   
        <div>
   @*@Html.Editor(Model.Items)*@
            @*@Html.CheckBoxList("name", Model.Items.Select(it => new SelectListItem() { Selected = it.IsChecked, Text = it.Key }))*@
        </div>

    <p>
        <label>Output:</label>
        @using (Html.BeginForm("Save", "Constitution", FormMethod.Get)) 
        
        {
      
          <div class="margin_bottom10">
            @Html.CheckBoxListFor(model => model.Posted.Keys,
                model => model.AvailableItems,
                it => it.Id,
                it => it.Name,
                model => model.SelectedItems);
              </div>

        <p><input class="green" type="submit" value="POST to controller" /></p>
	    }
      
        <p>
</div>

@*@section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }*@
<script src="~/Scripts/jquery.validate-vsdoc.js"></script>
<script src="~/Scripts/jquery.validate.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.js"></script>
<script src="~/Scripts/jquery.validate.unobtrusive.min.js"></script>
<script>

</script>


